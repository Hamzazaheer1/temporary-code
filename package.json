{
  "name": "monique-powell",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo with Next.js frontend and Express backend",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@10.18.0"
}
